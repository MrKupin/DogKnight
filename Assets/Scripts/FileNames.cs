public static class FileNames
{
    public static string GameScene = "GameScene";

    public enum SelectedItems
    {
        SelectedSword
    }

    public enum MoneyVaults
    {
        Coins,
        Crystals
    }
}
